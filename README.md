# 映像製作所 (Image Creator Workshop)

![Version](https://img.shields.io/badge/version-0.2.0-blue) ![License](https://img.shields.io/badge/license-MIT-green) ![React](https://img.shields.io/badge/React-19-61dafb) ![TypeScript](https://img.shields.io/badge/TypeScript-5.9-3178c6) ![Vite](https://img.shields.io/badge/Vite-7-646cff) ![TailwindCSS](https://img.shields.io/badge/TailwindCSS-v4-38bdf8) ![shadcn/ui](https://img.shields.io/badge/shadcn%2Fui-latest-000000)

**映像製作所**是一個專業、穩定且可控的 Web AI 影像生成工作台。透過整合 OpenAI、Azure OpenAI 與 Google Gemini 等多種 AI 模型，並搭配瀏覽器擴充功能整合 ChatGPT / Gemini 網頁版，讓使用者能透過自然語言描述與豐富的風格標籤，輕鬆創作出高品質的影像作品。

🔗 **Live Demo**：[eizo.ycsoft.dev](https://eizo.ycsoft.dev)

![Project Preview](./public/preview.png)

## 🌟 核心特色

### 🎨 多模型整合

- **OpenAI API**：直接連線 OpenAI，支援 DALL-E 3 等模型。
- **Azure OpenAI**：透過 Azure 部署的 OpenAI 服務，適用於企業場景。
- **Gemini API**：Google Gemini API，支援 Imagen 3.0 等模型。
- **ChatGPT 瀏覽器擴充**：透過瀏覽器擴充功能（如 ChatGPT 萬能工具箱），自動在 ChatGPT 網頁版填入提示詞並送出。
- **Gemini 瀏覽器擴充**：同樣支援 Gemini 網頁版的擴充功能整合。

### 🖥️ 專業級 IDE 式佈局

- **Header Bar**：頂部橫列顯示產品名稱、版本號、Provider 快速切換、模型名稱、連線狀態指示燈，以及系統設定與主題切換按鈕。
- **雙欄工作區**：左側 30% 為可折疊的控制面板 (Control Panel)，右側 70% 為結果預覽與歷史紀錄區域 (Result Area)。
- **響應式設計**：行動裝置自動切換為上下堆疊佈局。
- **Dark / Light 主題**：支援亮色、暗色與跟隨系統三種主題模式，偏好設定自動記憶。

### ✍️ 智慧提示詞輔助

- 內建 **12 組精選情境範本**（晨光咖啡館、雲端浮島、童話氣球遊行、水彩山嵐湖、像素霓虹街機、3D 爆炸結構、電影感追逐、復古旅行海報、玻璃未來展廳、陶瓷茶具特寫、布料質感時裝、紙雕城市模型）。
- **💡 靈感推薦**：一鍵隨機選取範本，降低創作門檻。

### 🏷️ 風格標籤系統

提供 **8 大類** 專業風格標籤，一鍵套用：

| 類別    | 標籤範例                                   |
| ------- | ------------------------------------------ |
| 🎬 構圖 | 廣角、特寫、極簡、平面、史詩、概念藝術     |
| 🎭 主題 | 賽博朋克、奇幻、自然、未來感、復古         |
| 🧱 材質 | 玻璃感、金屬感、紙質感、布料感、陶瓷感     |
| 📸 鏡頭 | 微距、移軸、散景、長焦、魚眼               |
| 🎨 畫風 | 寫實、插畫、繪本、水彩、像素風、3D、電影感 |
| 😊 情緒 | 溫暖、夢幻、活力、安靜、可愛               |
| 💡 光線 | 柔和光、棚拍、黃金時刻、霓虹、高對比       |
| 🎨 色彩 | 粉彩、繽紛、低彩度、單色、乾淨             |

### 🖼️ 全面輸出控制

- **畫面比例**：方形 (1024×1024)、直式 (1024×1536)、橫式 (1536×1024)、自動。
- **解析度**：低 / 中 / 高 / 自動。
- **檔案格式**：PNG / JPEG / WEBP。
- **壓縮品質**：可調整的滑桿控制 (0~100)。
- **安全等級**：較寬鬆 / 標準 / 較嚴格。

### 📷 參考圖輔助

- 支援**拖曳上傳**或點擊上傳參考圖片。
- 圖片轉為 Base64 後傳送給 AI，讓模型更精準理解構圖需求。

### 📜 歷史紀錄

- 自動記錄最近 **10 筆** 生成紀錄（含提示詞、風格標籤、畫面比例、使用模型）。
- 支援**一鍵還原**歷史紀錄的提示詞與設定。
- 單筆刪除或清空全部紀錄。
- 紀錄持久化至 LocalStorage，重新開啟瀏覽器不會遺失。

### 🔒 安全與隱私

- API Key 僅儲存於本地瀏覽器 (LocalStorage)，**不經過任何中介伺服器**。
- 純靜態前端應用，所有 API 呼叫直接從瀏覽器發送至各服務端點。

## 🛠️ 技術棧

本專案採用現代化前端技術構建，確保高效能與最佳開發體驗：

| 類別         | 技術                                                                                                                                    |
| ------------ | --------------------------------------------------------------------------------------------------------------------------------------- |
| **核心框架** | React 19 + TypeScript 5.9                                                                                                               |
| **建置工具** | Vite 7                                                                                                                                  |
| **樣式系統** | Tailwind CSS v4 + shadcn/ui + Radix UI                                                                                                  |
| **圖示庫**   | Lucide React                                                                                                                            |
| **UI 元件**  | Button, Card, Dialog, Select, Slider, Switch, Tabs, Toggle, Tooltip, DropdownMenu, ScrollArea, Badge, Separator 等 17 個 shadcn/ui 元件 |
| **狀態管理** | React Hooks (useState / useCallback / useMemo) + 自訂 Hooks                                                                             |
| **日期處理** | date-fns + zhTW Locale                                                                                                                  |
| **字型**     | Inter + Noto Sans TC (Google Fonts)                                                                                                     |
| **部署**     | GitHub Pages (gh-pages) + 自訂網域 (CNAME)                                                                                              |
| **架構**     | 純靜態網站 (Static Web App)，無後端伺服器                                                                                               |

## 🚀 快速開始

### 前置需求

請確保您的環境已安裝 [Node.js](https://nodejs.org/) (建議 v18 以上)。

### 安裝步驟

1. **複製專案**

   ```bash
   git clone https://github.com/YanchiHuang/image-creator-workshop.git
   cd image-creator-workshop
   ```

2. **安裝依賴**

   ```bash
   npm install
   ```

3. **啟動開發伺服器**

   ```bash
   npm run dev
   ```

   瀏覽器將自動開啟 `http://localhost:5173`。

4. **建置生產版本**

   ```bash
   npm run build
   ```

   建置後的檔案將位於 `dist` 目錄。

5. **部署至 GitHub Pages**

   ```bash
   npm run deploy
   ```

   此指令會自動執行 `build` 後透過 `gh-pages` 套件部署至 GitHub Pages。

## 📖 使用指南

### 1. 系統設定 (Settings)

首次使用請點擊 Header Bar 右側的「⚙️ 系統設定」圖示：

- 選擇您偏好的 **Provider**（OpenAI / Azure / Gemini API / ChatGPT / Gemini）。
- 輸入對應的 **API Key**、**Endpoint** 與 **Model** 等參數。
- 設定完成後，系統會自動儲存至瀏覽器 LocalStorage。

> 💡 也可以直接在 Header Bar 中間的 Provider 下拉選單快速切換連線方式。

### 2. 輸入提示詞 (Prompt)

- 在左側控制面板的「提示詞」區塊輸入您想要生成的畫面描述。
- 或點擊「💡 靈感推薦」隨機生成一個有趣的場景。
- 亦可展開「常用範本」列表，直接選用範本快速填入。

### 3. 風格與輸出調整

- 展開「風格標籤」區塊，點選各類別下的標籤來強化視覺效果。
- 展開「輸出設定」區塊，調整畫面比例、解析度、檔案格式與壓縮品質。
- 可上傳參考圖片，供 AI 輔助理解構圖需求。

### 4. 生成影像

- 在右側結果區域點擊「🚀 開始生成」按鈕。
- 生成過程中會即時顯示耗時計時器。
- 完成後，影像將顯示於結果區，可放大預覽或下載保存。
- 生成紀錄自動保存至下方的「歷史紀錄」區塊。

### 5. 瀏覽器模式 (ChatGPT / Gemini)

- 選擇 ChatGPT 或 Gemini Provider 時，點擊生成按鈕將自動開啟新分頁。
- 提示詞會透過 URL Hash 參數自動填入並送出（需搭配對應的瀏覽器擴充功能）。
- 結果區會顯示一個 Placeholder 提示圖，請在新分頁中查看實際生成結果。

## 📂 專案結構

```
image-creator-workshop/
├── public/
│   ├── CNAME                    # 自訂網域設定 (eizo.ycsoft.dev)
│   ├── preview.png              # 專案預覽圖
│   └── vite.svg                 # Favicon
├── src/
│   ├── components/
│   │   ├── ui/                  # shadcn/ui 基礎元件（17 個）
│   │   │   ├── badge.tsx
│   │   │   ├── button.tsx
│   │   │   ├── card.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── dropdown-menu.tsx
│   │   │   ├── input.tsx
│   │   │   ├── label.tsx
│   │   │   ├── scroll-area.tsx
│   │   │   ├── select.tsx
│   │   │   ├── separator.tsx
│   │   │   ├── slider.tsx
│   │   │   ├── switch.tsx
│   │   │   ├── tabs.tsx
│   │   │   ├── textarea.tsx
│   │   │   ├── toggle-group.tsx
│   │   │   ├── toggle.tsx
│   │   │   └── tooltip.tsx
│   │   ├── collapsible-panel.tsx # 可折疊面板（Radix Collapsible）
│   │   ├── control-panel.tsx    # 左側控制面板（Prompt / Style / Output / Reference）
│   │   ├── footer.tsx           # 頁尾
│   │   ├── generate-section.tsx # 生成按鈕（舊版保留）
│   │   ├── header-bar.tsx       # 頂部 Header Bar（Provider / Model / Status）
│   │   ├── hero-section.tsx     # 頁頭區塊（舊版保留）
│   │   ├── history-section.tsx  # 歷史紀錄區塊（舊版保留）
│   │   ├── mode-toggle.tsx      # 亮/暗/系統 主題切換
│   │   ├── output-section.tsx   # 輸出設定區塊（舊版保留）
│   │   ├── prompt-section.tsx   # 提示詞區塊（舊版保留）
│   │   ├── result-area.tsx      # 右側結果區域（生成按鈕 + 預覽 + 歷史紀錄）
│   │   ├── settings-modal.tsx   # 系統設定彈窗（Dialog）
│   │   └── theme-provider.tsx   # 主題 Context Provider
│   ├── lib/
│   │   ├── services/
│   │   │   ├── azure.ts         # Azure OpenAI API 服務
│   │   │   ├── browser-extension.ts # 瀏覽器擴充功能整合（ChatGPT / Gemini）
│   │   │   ├── gemini.ts        # Google Gemini API 服務
│   │   │   ├── image-generator.ts # 影像生成統一入口（策略模式）
│   │   │   ├── openai.ts        # OpenAI API 服務
│   │   │   └── types.ts         # 共用型別定義（GenerateOptions / GenerateResult / APIError）
│   │   ├── constants.ts         # 常數定義（範本 / 風格標籤 / 連線選項 / 輸出選項）
│   │   ├── store.ts             # 狀態管理（5 個自訂 Hook + LocalStorage 持久化）
│   │   └── utils.ts             # 工具函式（cn）
│   ├── App.tsx                  # 主應用程式入口
│   ├── main.tsx                 # React DOM 渲染入口
│   └── index.css                # 全域樣式、Tailwind v4 + shadcn/ui 色彩系統、佈局
├── index.html                   # HTML 入口檔案（含 SEO Meta、Google Fonts）
├── vite.config.ts               # Vite 設定檔（含 @tailwindcss/vite 與路徑別名）
├── components.json              # shadcn/ui 設定檔
├── tsconfig.json                # TypeScript 根設定
├── tsconfig.app.json            # TypeScript 應用設定
├── tsconfig.node.json           # TypeScript Node 設定
├── eslint.config.js             # ESLint 設定
└── package.json                 # 專案依賴與腳本
```

## 🏗️ 架構設計

### 服務層 (Service Layer)

採用**策略模式 (Strategy Pattern)** 設計影像生成服務：

```
ImageGenerator (統一入口)
├── OpenAIImageService        → OpenAI API
├── AzureOpenAIImageService   → Azure OpenAI API
├── GeminiImageService        → Google Gemini API
└── BrowserExtensionImageService → ChatGPT / Gemini 瀏覽器擴充
```

所有服務實作共用的 `ImageGenerationService` 介面，透過 `connectionType` 自動路由至對應的服務。

### 狀態管理 (State Management)

使用 **5 個自訂 React Hooks** 管理應用狀態：

| Hook                 | 職責                                         | 持久化          |
| -------------------- | -------------------------------------------- | --------------- |
| `useAppSettings`     | API 連線設定（Provider / Key / Model）       | ✅ LocalStorage |
| `useOutputSettings`  | 輸出參數（比例 / 解析度 / 格式 / 品質）      | ✅ LocalStorage |
| `usePromptState`     | 提示詞 / 風格標籤 / 參考圖                   | ❌              |
| `useGenerationState` | 生成狀態（idle / generating / done / error） | ❌              |
| `useHistoryState`    | 歷史紀錄（最多 10 筆）                       | ✅ LocalStorage |

## 🤝 貢獻指南

歡迎提交 Pull Request 或 Issue 來協助改進本專案！

1. Fork 本專案
2. 建立您的 Feature Branch (`git checkout -b feature/AmazingFeature`)
3. 提交您的變更 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到 Branch (`git push origin feature/AmazingFeature`)
5. 開啟 Pull Request

## 📄 版權聲明

© 2026 映像製作所 (Image Creator Workshop). Built by Claude & Gemini.
